// Cursor auto-hide is temporarily disabled.
// The previous implementation registered document/window event listeners that
// triggered the recorder.js e.matches regression in the browser environment.
// Keep this module as an inert import so main.ts does not need another broad edit.
export {};
